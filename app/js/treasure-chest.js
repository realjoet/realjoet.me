$(function(){

  $('display-section__content-hero').click(function(){
    // var order = this.css('order');

    // this.parent().children().css({'order':(order++)});
    this.css('order': 1);
  })
})